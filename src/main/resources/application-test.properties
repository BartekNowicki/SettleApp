logging.level.org.springframework=DEBUG
logging.level.org.hibernate=DEBUG

spring.datasource.url=jdbc:h2:mem:testdb;MODE=MySQL
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

spring.jpa.hibernate.ddl-auto=create-drop

#For the H2 console to work, the H2 database must be running in server mode, which means it operates
# as a separate process from your main application. This separation enables you to access the database
# using external tools like the H2 console while your main application is running.
#it requires a separate setup with tcpip and the current tests would fail

#this should not be used but is needed to initialize the context
cors.allowedOrigins=http://localhost:5173

#key for signing the jwt token, generated with command: openssl rand -base64 64
jwt.secret=7tbM3+TcHVeoXgD7RClsEdBf85VCKWgv5tbC94j2uTKb4gz+uYelu472uwhhA5rzgB1Un2LT/0wzL/WamLp15Q==

